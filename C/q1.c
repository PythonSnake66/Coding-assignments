// 1. Write a program which reads in two characters and outputs them in reverse order

#include <stdio.h>
int main() {
    char a, b;
    scanf(" %c %c", &a, &b);
    printf("%c %c", b, a);
    return 0;
}
